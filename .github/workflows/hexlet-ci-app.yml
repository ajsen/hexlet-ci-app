name: hexlet-ci-app
on: [push, workflow_dispatch]

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: setup
        run: install build
      - name: start
        run: npm install
      - name: lint
        run: npx eslint .
      - name: test
        run: npm test
     
     
